[project]
name = "goochess"
version = "0.1.0"
description = "Semantically written chess game with a GUI built using pygame."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pygame",
]
